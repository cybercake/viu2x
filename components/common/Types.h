#ifndef TYPES_H
#define TYPES_H

#include "CompilationSettings.h"
#include "String.h"
#include <string>
#include <inttypes.h>

namespace viu2x {

    typedef double Real;

#define MIN(a, b) (a) < (b) ? (a) : (b)
#define MAX(a, b) (a) > (b) ? (a) : (b)

}

#endif // TYPES_H
